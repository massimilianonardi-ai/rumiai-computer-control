# MCP adapter

MCP exposes and discovers Computer Control capabilities; it does not define their
internal semantics. This adapter translates MCP tools to the canonical contract
and returns the canonical verified result envelope.

No OS-specific accessibility logic belongs in this package.
