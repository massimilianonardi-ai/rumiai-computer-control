# Local backend executables

Place the validated `agent-ctrl` executable here, or set the `AGENT_CTRL`
environment variable to an absolute executable path.

Generated Swift helper binaries are also written to this directory and are not
tracked by Git.
